// constants enum
export enum BUTTON_TYPE_CLASSES {
  add = 'add',
  remove = 'remove',
  complete = 'complete',
}

export enum TODOS_ACTION_TYPE {
  get = 'get',
  add = 'add',
  delete = 'delete',
  complete = 'complete',
}
